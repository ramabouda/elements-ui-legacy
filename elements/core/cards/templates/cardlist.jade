
div.cardlist
  div.card(
    data-card-id='{{ card.id }}'
    ng-repeat='card in cards track by card.id'
    class='{{ card.elements.join(" ") }}'
  )
    div.name {{ card.name }}
    div.cost
      span(ng-repeat='cost in card.cost') {{ cost.quantity }}
        i(class='eli-{{ cost.energy }}1')
    div.picture
    div.kind {{ card.kind }}
    div.rarity {{ card.rarity }}
    div.text blah blah blah blah blah blah dfqsef qsf qsef  sqfqs eq
